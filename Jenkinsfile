pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "build"'   // on Windows
            }
        }
        stage('Test') {
            steps {
                sh 'echo "test"'
            }
        }
    }
}
